extends registration_base
| {% load i18n %}

block title
	| Reset password

block content
	p Forgot your password?  Enter your email in the form below and we'll send you instructions for creating a new one.
	form(method="post" action="")
		| {% csrf_token %}
		#{form.as_p}
		input(type="submit" value="Reset password")
			
